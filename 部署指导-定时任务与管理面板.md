# 部署指导：Docker 测试部署（定时任务 + 管理面板版本）

> 适用版本：本仓库 `dabai214109/university-helper`（基于上游 v1.4.7 的二次开发）
> 目标读者：① 宝塔面板的 AI 助手 ② 手动操作的运维/开发者
> 部署方式：**从源码构建 Docker 镜像**（本 fork 的改动不在 GHCR 官方镜像里）

---

## ⚠️ 丢给宝塔 AI 之前：请先确认它知道源码在哪

这份文档假定**源码已经在服务器上**。宝塔 AI 最容易在这里出错，所以先给它下面这段
（连同文档一起发过去）：

```
本项目源码必须来自这个 fork，不要 clone 上游：

    git clone https://github.com/dabai214109/university-helper.git /www/wwwroot/university-helper
    cd /www/wwwroot/university-helper

如果服务器 git clone GitHub 超时（国内很常见：HTTP 通、git 长连接被阻断），
改用 zip 上传 —— 详见 §1.5「源码上传（git 不通时）」：

    1. 在能上网的机器上执行：python scripts/make_deploy_zip.py
       得到 uh-deploy-src.zip（约 1.2 MB）
    2. 宝塔文件管理器上传到 /www/wwwroot/，解压到 university-helper/
    3. 继续执行方案 A，不需要 .git 目录

验证源码是不是本 fork（比检查 .git 更可靠）：

    grep -q dispatch_scheduled_tasks \
      backend/app/services/course/chaoxing/learning_manager.py \
      && echo "✅ 本 fork" || echo "❌ 拿错了源码"

不要 clone sweetcornna/university-helper —— 那是上游，没有定时任务和管理面板。
不要直接 docker compose -f docker-compose.release.yml up —— 那是上游预构建镜像。
```

**为什么必须强调这一点**：

| 如果 AI 这样做 | 后果 |
|---|---|
| `git clone https://github.com/sweetcornna/university-helper.git` | ❌ 拿到上游代码，**没有**定时任务和管理面板 |
| ✅ `git clone https://github.com/dabai214109/university-helper.git` | 正确 |
| ✅ **zip 上传**本 fork 源码到 `/www/wwwroot/university-helper` | **同样正确**，方案 A / B 都能用 |
| `docker compose -f docker-compose.release.yml up` | ❌ 拉上游 GHCR 镜像，**不含**本 fork 改动 |

> **关于 `.git` 的澄清**（重要）：`deploy_server.sh` **不要求** `.git` 存在。
> 我核对过脚本源码：
> - `resolve_repo_root()`（第 184–201 行）只检查上面那 4 个文件是否存在。齐全就直接用当前
>   目录，**不会**触发 `bootstrap_source` 联网下载。
> - `refresh_source_if_outdated()`（第 292–301 行）只有在目录里存在 `.uh-source-tag`
>   文件时才会考虑重新下载；zip 解压出来的目录**没有**这个文件，所以第 300 行直接
>   `return 0`，**不会**覆盖你的源码。
>
> 所以：**git clone 和 zip 上传都可以走方案 A**。`--build` 模式构建的是**当前目录**的源码，
> 与是否有 `.git` 无关。唯一的差别是以后更新代码时，git checkout 能 `git pull`，
> zip 需要重新上传。

> 本 fork 是**公开仓库**，clone 无需认证。

---

## 0. 给宝塔 AI 的执行摘要（先读这一段）

如果你是被要求「部署这个项目到 Docker」的 AI 助手，按以下顺序执行，不要跳步：

**第 0 步：确认源码已就位**

```bash
cd /www/wwwroot/university-helper 2>/dev/null || {
  echo "目录不存在，先获取源码（二选一）："
  echo "  A) git clone（服务器能连 GitHub 时）"
  echo "     git clone https://github.com/dabai214109/university-helper.git /www/wwwroot/university-helper"
  echo "  B) git 超时（HTTP 通但长连接被阻断）→ 让用户上传源码 zip，见 §1.5"
  echo "     用户在能上网的机器执行: python scripts/make_deploy_zip.py"
  echo "     得到 uh-deploy-src.zip (~1.2MB)，宝塔文件管理器上传到 /www/wwwroot/ 后解压"
  exit 1
}
cd /www/wwwroot/university-helper

# 确认是本 fork 而非上游（这比检查 .git 可靠）
grep -q "dispatch_scheduled_tasks" \
  backend/app/services/course/chaoxing/learning_manager.py \
  && echo "✅ 是本 fork（含定时任务代码）" \
  || { echo "❌ 这不是本 fork！缺定时任务代码，请重新获取源码"; exit 1; }

# 确认脚本不会联网下载（这 4 个文件齐全即可，不需要 .git）
for f in docker-compose.release.yml database/00-schema.sql \
         database/02-bootstrap-tenant-template.sh database/templates/tenant_template.sql; do
  [ -f "$f" ] || { echo "❌ 缺少 $f —— 脚本会尝试联网下载"; exit 1; }
done
echo "✅ 源码完整"
```

> **注意**：`deploy_server.sh` **不要求 `.git`**。只要上面 4 个文件齐全，脚本就用当前目录的
> 源码构建，不会联网、不会覆盖。**zip 上传同样能走方案 A**（见 §1.5）。
>
> **git clone 超时是常见情况**（国内服务器 HTTP 通、git 443 长连接被阻断）。遇到就转 §1.5，
> 不要反复重试，也不要改用上游源码。

**第 1 步：选方案**

```bash
# 服务器上是否已有其它 university-helper 部署？
docker ps -a --format '{{.Names}}' | grep -E 'shuake-easy-learning|university-helper' || echo "无冲突"
```

- **无冲突** → 走**方案 A**（官方脚本，密钥/TLS 全自动）
- **有冲突** → 走**方案 B**（本文件的 compose，容器名 `uh-test-*` 隔离）

**方案 A（推荐）**：

1. 确认第 0 步通过（源码完整）
2. `bash scripts/deploy_server.sh --build --host <服务器IP> --admin-email <邮箱> -y`
3. 等 2–5 分钟（构建前端 + 后端镜像）
4. `curl -fsS http://127.0.0.1:8080/health`，确认 `"schema":"ok"`
5. 往 `.env` 追加 `UPDATE_CHECK_ENABLED=false`（否则会提示升级到上游版本），重启 app
6. 报告用户：访问地址、管理员邮箱、§6 冒烟测试步骤

**方案 B**：见 §3.1 与附录 A.2（需手动生成三个密钥）。

**禁止事项**：
- ❌ 不要 clone 上游 `sweetcornna/university-helper`（没有本 fork 的改动）
- ❌ 不要用 `docker-compose.release.yml` 直接 `up`（拉的是上游镜像）
- ❌ 不要把 app 的 `--workers` 改成大于 1（会导致定时调度分裂，见 §9）
- ❌ 不要覆盖已有的 `.env` 或删除数据卷（会丢失账号与已存凭据）

---

## 1. 部署架构说明

本测试栈由三个容器组成：

```
                    ┌─────────────────────────────────────┐
   浏览器  ──────▶  │  web (nginx:1.27-alpine)            │
   (8080/HTTPS)     │  静态 SPA + 反代 /api/ /health /ws   │
                    └──────────────┬──────────────────────┘
                                   │ app:8000 (内部网络)
                    ┌──────────────▼──────────────────────┐
                    │  app (FastAPI + uvicorn, workers=1) │
                    │  定时调度循环 / 任务管理 / 管理 API   │
                    └──────────────┬──────────────────────┘
                                   │ postgres:5432
                    ┌──────────────▼──────────────────────┐
                    │  postgres:15-alpine                 │
                    │  main_db + tenant_* 租户库           │
                    └─────────────────────────────────────┘
```

| 容器名（方案 B） | 容器名（方案 A） | 镜像 | 端口 | 说明 |
|---|---|---|---|---|
| `uh-test-web` | `shuake-easy-learning-web` | 由 `Dockerfile.web` 构建 | `HTTP_PORT`（默认 8080） | 前端 + 边缘反代 |
| `uh-test-app` | `shuake-easy-learning-app` | 由 `Dockerfile.server` 构建 | `127.0.0.1:8000` | 后端 API |
| `uh-test-db` | `shuake-easy-learning-db` | `postgres:15-alpine` | 仅内部 | 数据库 |

### 为什么必须从源码构建

上游的 `docker-compose.release.yml` 从 GHCR 拉取 `ghcr.io/sweetcornna/university-helper-{app,web}`，
**那些镜像里没有本次二次开发的代码**（定时任务排期、管理面板）。

上游 `README.zh-CN.md` 里提到 web 的地方有三处，都要注意：

| README 位置 | 内容 | 对本 fork 是否可用 |
|---|---|---|
| 「一键部署服务端」 | 启动 Postgres + 后端 + **web 容器** | ⚠️ 默认拉上游镜像，**不含**本 fork 改动 |
| 「部署细节」 | `ghcr.io/sweetcornna/university-helper-web`（nginx + 构建好的前端） | ❌ 上游镜像，不含本 fork 改动 |
| 「手动部署（从源码构建）」 | 宿主机 nginx + `server.yml`，**需宿主机装 Node 20** 构建 `frontend/dist/` | ✅ 能构建本 fork，但宿主机要有 Node，且只有 app+postgres |

### 三条可用路径对比

> 术语约定：**方案 A** = 官方脚本 `deploy_server.sh --build`；**方案 B** = 本文件的
> `docker-compose.test.yml`；**方案 C** = README 的手动构建路径。
> （下文 §3/§4 的「方式一/方式二」指的是*对外访问方式*：IP 直连 vs 域名反代，
> 与这里的方案 A/B/C 是两个维度，可自由组合。）

| | 方案 A. 官方脚本 `--build` | 方案 B. 本文件 compose | 方案 C. README 手动路径 |
|---|---|---|---|
| 构建的是本 fork 代码 | ✅ | ✅ | ✅ |
| 宿主机需装 Node 20 | ❌ 不需要 | ❌ 不需要 | ✅ **需要** |
| 前端由谁服务 | 容器内 nginx | 容器内 nginx | 宿主机 nginx 读 `dist/` |
| 启用服务 | app + web + postgres | app + web + postgres | 仅 app + postgres |
| 自动生成密钥 | ✅ | ❌ 手动填 `.env` | ❌ 手动填 `.env` |
| 自动生成 TLS 模板 | ✅（`--domain`） | ❌ | ❌ |
| 容器名 / 卷名 | `shuake-easy-learning-*` | `uh-test-*`（**与生产隔离**） | `shuake-easy-learning-*` |
| 会改写 `.env` | ✅ 脚本接管 | ❌ | ❌ |

**推荐选择**：

- 服务器上**没有**其它 university-helper 部署 → **用方案 A**（最省事，密钥/TLS 模板全自动）：
  ```bash
  cd <fork 目录>          # 必须是 git checkout，见下方风险
  bash scripts/deploy_server.sh --build --host <服务器IP> --admin-email you@example.com -y
  ```
- 服务器上**已有**生产部署，或想完全隔离、自己掌控 `.env` → **用方案 B**（见 §3.1/§4）。
- 只有想省掉容器内 nginx、让宿主机 nginx 直接托管静态文件时 → 用方案 C（但要装 Node）。

> **方案 A 是否要求 `.git`？不要求。** 我核对过 `scripts/deploy_server.sh`：
> - `resolve_repo_root()`（第 184–201 行）只检查 `docker-compose.release.yml` 和
>   `database/` 下 3 个文件是否存在。齐全就用当前目录，**不会**触发联网下载。
> - `refresh_source_if_outdated()`（第 292–301 行）只在目录里有 `.uh-source-tag` 时
>   才考虑重新下载；zip 解压出来的目录没有这个文件，所以直接 `return 0`。
>
> 因此 **git clone 与 zip 上传都能走方案 A**，构建的是当前目录的源码。唯一差别是
> 后续更新：git checkout 可 `git pull`，zip 需重新上传。

- ❌ 无论哪条路径，都**不要**直接 `docker compose -f docker-compose.release.yml up`（拉上游镜像）

---

## 1.5 源码上传（git 不通时）

国内服务器常见的现象：**HTTP 能通，但 `git clone` 在 443 上挂住直到超时**。
这不是服务器不能上网，而是 GitHub 的 git 长连接被阻断。此时用 zip 上传，**方案 A 照样能用**。

### 步骤 1：在能上网的机器上打包

在**本仓库的 git checkout 目录**里执行：

```bash
python scripts/make_deploy_zip.py
```

输出：

```
wrote /path/to/uh-deploy-src.zip
  1.15 MB   531 files
...
All checks passed.
```

脚本会**自动校验**三件事，任何一项不过就以非零码退出：

| 校验 | 意义 |
|---|---|
| 4 个 GUARD 文件齐全 | 脚本不会尝试联网下载、不会覆盖源码 |
| 构建所需的 Dockerfile / nginx / package-lock 齐全 | `--build` 能跑通 |
| zip 内含 `dispatch_scheduled_tasks`、`admin_overview` 等标记 | **确认是本 fork，不是上游** |

> 只打包 git 跟踪的文件，所以 `node_modules/`（194 MB）、`frontend/dist/`、
> `.npm-cache/` 都不会进去。`.git/`（约 150 MB）也排除——脚本不需要它。
>
> 打包脚本用 `git ls-files -z` 读取文件名。这一点很关键：默认的
> `core.quotepath` 会把中文名转义成八进制（`\351\203\250...`），
> 导致中文文件名（如本指导文档）在后续字符串比较中**静默丢失**。

### 步骤 2：上传并解压

宝塔面板 →「文件」→ 进入 `/www/wwwroot/` → 上传 `uh-deploy-src.zip`。

然后任选一种方式解压：

```bash
# 方式一：宝塔文件管理器右键 → 解压（解压到 /www/wwwroot/university-helper）
# 方式二：命令行
cd /www/wwwroot
mkdir -p university-helper
unzip -q uh-deploy-src.zip -d university-helper
cd university-helper
```

> ⚠️ **注意目录层级**：zip 里是**相对路径**（`backend/...`、`scripts/...`），
> 没有顶层目录。所以必须解压**到一个目录里**（如 `university-helper/`），
> 不要直接解压到 `/www/wwwroot/`，否则会把 `backend/`、`nginx/` 等散落到上层。

### 步骤 3：验证

```bash
cd /www/wwwroot/university-helper

# 1) 确认是本 fork
grep -q "dispatch_scheduled_tasks" \
  backend/app/services/course/chaoxing/learning_manager.py \
  && echo "✅ 本 fork（含定时任务）" || echo "❌ 拿错源码了"

# 2) 确认脚本不会联网下载（这 4 个文件齐全即可）
for f in docker-compose.release.yml database/00-schema.sql \
         database/02-bootstrap-tenant-template.sh database/templates/tenant_template.sql; do
  [ -f "$f" ] || echo "❌ 缺少 $f"
done
echo "✅ 源码完整"

# 3) 确认没有把 node_modules 之类带上来
ls -d frontend/node_modules 2>/dev/null && echo "⚠️ 有 node_modules（非必需，可删）" || echo "✅ 干净"
```

### 步骤 4：继续部署

```bash
bash scripts/deploy_server.sh --build --host <服务器IP> --admin-email you@example.com -y
```

**唯一代价**：以后更新代码时，zip 需要重新打包上传；git checkout 可以 `git pull`。

### 更新流程（zip 方式）

```bash
# 本地：重新打包
python scripts/make_deploy_zip.py

# 上传覆盖（宝塔文件管理器），然后服务器上：
cd /www/wwwroot/university-helper
docker compose -p university-helper -f docker-compose.release.yml up -d --build
```

> `.env` 和数据卷不受影响，**不要**执行 `down -v`。

---

## 2. 部署前准备

> 如果你选**方案 A（官方脚本）**，脚本会自动生成密钥并写 `.env`，可以直接跳到
> **§3.0**。下面的 §2.1–2.3 主要给方案 B / C 用。

### 2.1 检查环境

```bash
# Docker 与 Compose 版本（需要 Compose v2，即 docker compose 而非 docker-compose）
docker --version
docker compose version

# 磁盘空间：镜像 + 数据卷约需 3–5 GB
df -h /var/lib/docker
```

若 `docker compose version` 报错，宝塔面板可在「软件商店 → Docker管理器」中安装/更新。

### 2.2 生成密钥

```bash
# JWT 签名密钥（32 字节 hex）
openssl rand -hex 32

# 平台凭据加密密钥（Fernet）—— 必须用这个格式
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

如果宿主机没有 `cryptography`，用 Docker 生成：

```bash
docker run --rm python:3.11-slim sh -c \
  "pip install -q cryptography && python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\""
```

> ⚠️ `CREDENTIAL_ENCRYPTION_KEY` 用来加密定时任务落库的账号密码与题库 token。
> **丢失它，已保存的凭据将无法解密**。请和 `.env` 一起备份。

### 2.3 创建 .env

```bash
cd <项目根目录>
cp .env.test.example .env
vi .env          # 或宝塔面板的文件管理器直接编辑
```

必填项对照：

| 变量 | 是否必填 | 说明 |
|---|---|---|
| `POSTGRES_PASSWORD` | ✅ | 自定义强密码 |
| `SECRET_KEY` | ✅ | ≥32 字符，§2.2 生成 |
| `CREDENTIAL_ENCRYPTION_KEY` | ✅ | Fernet 格式，§2.2 生成 |
| `ADMIN_EMAILS` | ✅（管理面板） | 管理员邮箱，逗号分隔 |
| `CORS_ORIGINS` | ✅ | **JSON 数组**格式，如 `["http://1.2.3.4:8080"]` |
| `ALLOWED_HOSTS` | 按需 | 用域名/IP 访问时必填，否则 400 |
| `HTTP_BIND_HOST` | 按需 | 用 IP 直连测试时改 `0.0.0.0` |
| `ENV` | 按需 | IP 测试用 `dev`；域名+HTTPS 用 `production` |

**关于 `CORS_ORIGINS` 的常见错误**：它必须是 JSON 数组，不是逗号分隔字符串。

```bash
# ❌ 错：http://localhost:8080
# ❌ 错：http://localhost:8080,http://1.2.3.4:8080
# ✅ 对：["http://localhost:8080"]
# ✅ 对：["https://uh.example.com"]
```

---

## 3. 方式一：直接用 IP + HTTP 测试（最快）

### 3.0 方案 A：官方脚本一键部署（推荐，最省事）

上游的 `scripts/deploy_server.sh` 自带 `--build`，会从本 fork 源码构建
`Dockerfile.server` + `Dockerfile.web`，并自动生成 `.env` 里的随机密钥。

**前置检查（必做）**：

```bash
cd <fork 目录>

# 1) 确认是本 fork（含定时任务代码），不是上游
grep -q "dispatch_scheduled_tasks" \
  backend/app/services/course/chaoxing/learning_manager.py \
  && echo "✅ 是本 fork" || echo "❌ 不是本 fork，请重新获取源码"

# 2) 确认脚本不会联网下载（这 4 个文件齐全即可，不需要 .git）
for f in docker-compose.release.yml database/00-schema.sql \
         database/02-bootstrap-tenant-template.sh database/templates/tenant_template.sql; do
  [ -f "$f" ] || echo "❌ 缺少 $f —— 脚本会尝试联网下载"
done
echo "✅ 检查完成"
```

> `deploy_server.sh` **不要求 `.git`**，zip 上传的目录同样能走方案 A（详见文首说明）。

**执行**：

```bash
cd <fork 目录>

# 用 IP 走纯 http（ENV=dev）
bash scripts/deploy_server.sh --build --host <服务器IP> --admin-email you@example.com -y

# 或用域名（ENV=production + https CORS + 生成宿主机 nginx 模板）
bash scripts/deploy_server.sh --build --domain uh.example.com --admin-email you@example.com -y
```

脚本会：生成 `.env` → 构建两个镜像（打 `:local` tag）→ 启动 app + web + postgres →
等 `/health` 通过 → 校验数据库可注册。`--domain` 时还会在 `deploy/nginx/` 写 TLS 模板
并打印 `certbot` 命令。

**访问**：`http://<服务器IP>:8080`（或 `https://<域名>`）

**运维命令**（方案 A 用 `docker-compose.release.yml` + `-p university-helper`）：

```bash
docker compose -p university-helper -f docker-compose.release.yml ps
docker compose -p university-helper -f docker-compose.release.yml logs -f app
docker compose -p university-helper -f docker-compose.release.yml restart app
```

**改了代码要重新部署**：重新跑一次同一条 `--build` 命令即可（`.env` 和数据卷保留）。

> 注意：`--build` 模式给镜像打的 tag 是 `:local`，`docker compose ... pull` 会尝试拉
> `:local` 而失败，属正常现象，直接用 `up -d` 即可。

---

### 3.1 方案 B：用 IP + HTTP 测试（最快，与生产隔离）

适合「先跑起来看看」的联调场景，且不会与服务器上已有的 university-helper 部署冲突。

**.env 配置**：

```env
HTTP_BIND_HOST=0.0.0.0
HTTP_PORT=8080
ENV=dev
CORS_ORIGINS=["http://<你的服务器IP>:8080"]
ALLOWED_HOSTS=<你的服务器IP>
```

**启动**：

```bash
docker compose -p uh-test -f docker-compose.test.yml up -d --build
```

**放行防火墙**：宝塔面板「安全」→ 放行 `8080` 端口；云服务器还需在**云厂商安全组**放行。

**访问**：`http://<服务器IP>:8080`

> ⚠️ 明文 HTTP 传输，仅用于测试。测试完请改用方式二并关闭 8080 对外。

---

## 4. 方式二：宝塔 nginx 反代 + 域名（推荐）

> 方案 A 与方案 B 都适用，区别只是容器名与 compose 文件名：
> - 方案 A（官方脚本）→ `-p university-helper -f docker-compose.release.yml`
> - 方案 B（本文件）→ `-p uh-test -f docker-compose.test.yml`
> 下面以方案 B 为例，方案 A 把命令里的项目名和文件名替换掉即可。

### 4.1 启动容器（仅绑回环）

```env
HTTP_BIND_HOST=127.0.0.1
HTTP_PORT=8080
ENV=production
CORS_ORIGINS=["https://uh.example.com"]
ALLOWED_HOSTS=uh.example.com
```

```bash
docker compose -p uh-test -f docker-compose.test.yml up -d --build
```

### 4.2 宝塔新建站点

1. 宝塔面板 →「网站」→「添加站点」
   - 域名：`uh.example.com`
   - PHP 版本：**纯静态**
   - 不要创建数据库、不要创建 FTP
2. 站点创建后 →「设置」→「SSL」→ Let's Encrypt 申请证书 → 开启「强制 HTTPS」

### 4.3 配置反向代理

站点「设置」→「反向代理」→「添加反向代理」：

| 项 | 值 |
|---|---|
| 代理名称 | `uh` |
| 目标 URL | `http://127.0.0.1:8080` |
| 发送域名 | `$host` |

宝塔会生成一段 nginx 配置。**关键**：确认它包含 WebSocket 与长连接支持，
把生成的配置改成下面这样（宝塔默认配置缺少 `Upgrade` 与长超时，
会导致任务日志轮询和签到 WebSocket 失败）：

```nginx
location / {
    proxy_pass http://127.0.0.1:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # WebSocket（学习通签到、任务日志推送需要）
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # 刷课任务接口可能长时间等待，避免 504
    proxy_read_timeout 300s;
    proxy_connect_timeout 75s;
    proxy_send_timeout 300s;
    client_max_body_size 20M;
}
```

> 注意：web 容器**内部**已有一层 nginx 处理了静态资源缓存、CSP 头、限流。
> 宝塔这层只做 TLS 终结与转发即可，**不要再叠加限流**，否则可能出现 429。

### 4.4 访问

`https://uh.example.com`

---

## 5. 验证部署

### 5.1 容器状态

```bash
docker compose -p uh-test -f docker-compose.test.yml ps
```

三个容器都应为 `running`（healthy）。`app` 的 HEALTH 列显示 `healthy` 才算真正就绪。

### 5.2 健康检查

```bash
# 通过 web 容器（宝塔反代的目标）
curl -fsS http://127.0.0.1:8080/health

# 直连 app（绕过 web，用于区分是哪一层的问题）
curl -fsS http://127.0.0.1:8000/health
```

期望输出：

```json
{"status":"ok","db":"ok","cleanup_task":"alive","schema":"ok"}
```

**关键看 `schema` 字段**：

| 值 | 含义 | 处理 |
|---|---|---|
| `ok` | 正常，可以注册 | — |
| `missing_users` | 缺 users 表 | 见 §8.2 |
| `missing_tenant_template` | 缺租户模板库 | 见 §8.2 |
| `unknown` | 数据库连不上 | 检查 postgres 容器日志 |

### 5.3 首次注册（必须）

打开站点 → 注册账号，**邮箱必须与 `.env` 里的 `ADMIN_EMAILS` 一致**，否则看不到管理面板。

> 如果 `ADMIN_EMAILS` 留空，则**第一个注册的账号**自动成为管理员。

### 5.4 确认新功能已生效

```bash
# 1) 管理面板接口存在（未登录应返回 401/403，而不是 404）
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/v1/admin/overview
# 期望：401 或 403（说明路由已挂载）；若是 404 说明代码没构建进去

# 2) 定时任务字段已生效（未登录同样应返回 401/403）
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://127.0.0.1:8080/api/v1/course/task/test/reschedule
# 期望：401 或 403
```

若想看到完整接口列表，在 `.env` 设 `DOCS_ENABLED=true` 并重启 app，然后访问 `/docs`
（**仅测试期开启，生产请关闭**）。

---

## 6. 功能测试步骤（定时任务 + 管理面板）

### 6.1 定时任务冒烟测试

1. 登录 → 「学习通泛雅」→ 填写账号密码 → 选择课程
2. 在「执行方式」选 **定时启动**
3. 启动时间设为 **当前时间 + 2 分钟**，波动设为 `0`
4. 点击「开始刷课」→ 应提示「已加入定时队列」，任务卡片显示 **定时中** 徽标
5. 观察：
   - 后端日志：`docker compose -p uh-test -f docker-compose.test.yml logs -f app`
   - **15 秒内**（调度循环周期）任务应自动从 `scheduled` 变为 `running`
6. 测试其他按钮：
   - **改期**：改到更晚时间 → 状态回到 `scheduled`，`fire_at` 重置
   - **立即开始**：跳过等待，15 秒内起跑
   - **停止**：定时任务直接变 `cancelled`（**定时状态下不显示「暂停」按钮**，这是预期行为）
7. 测试到点自动暂停：设 `start_at = now+1min`、`stop_at = now+3min`，观察 3 分钟后自动变 `paused`

### 6.2 管理面板测试

1. 用 `ADMIN_EMAILS` 里的邮箱登录
2. 顶部导航应出现「**管理面板**」入口（本地模式不显示，服务器模式才有）
3. 进入 `/admin`，检查：
   - 4 个 KPI 卡片（用户数 / 运行中 / 定时队列 / 24h 失败）
   - 用户管理表
   - 任务总览表：可用状态下拉筛选、刷新、对活跃任务点「停止」
4. 用**非管理员**账号登录 → 访问 `/admin` → 应显示「需要管理员权限」友好空态

### 6.3 重启恢复测试（重要）

定时任务的排期需要能跨重启恢复：

```bash
# 1) 创建一个 10 分钟后的定时任务
# 2) 重启 app 容器
docker compose -p uh-test -f docker-compose.test.yml restart app
# 3) 等待容器 healthy 后，刷新页面
# 4) 定时任务应仍在列表中，状态仍为 scheduled（不会被标为 failed）
```

---

## 7. 常用运维命令

```bash
# 以下命令都在项目根目录执行，-p uh-test 是项目名

# 查看状态
docker compose -p uh-test -f docker-compose.test.yml ps

# 查看日志
docker compose -p uh-test -f docker-compose.test.yml logs -f app
docker compose -p uh-test -f docker-compose.test.yml logs -f web
docker compose -p uh-test -f docker-compose.test.yml logs --tail=100 postgres

# 重启单个服务
docker compose -p uh-test -f docker-compose.test.yml restart app

# 停止（保留数据）
docker compose -p uh-test -f docker-compose.test.yml down

# 停止并删除数据卷（⚠️ 清空所有账号与任务数据）
docker compose -p uh-test -f docker-compose.test.yml down -v

# 代码更新后重新构建部署
git pull
docker compose -p uh-test -f docker-compose.test.yml up -d --build

# 只重建后端
docker compose -p uh-test -f docker-compose.test.yml up -d --build app

# 只重建前端（改了 frontend/ 下代码）
docker compose -p uh-test -f docker-compose.test.yml up -d --build web

# 进入数据库
docker compose -p uh-test -f docker-compose.test.yml exec postgres \
  psql -U easylearning -d main_db

# 查看任务表（验证 scheduled 状态落库）
docker compose -p uh-test -f docker-compose.test.yml exec postgres \
  psql -U easylearning -d main_db -c \
  "SELECT task_id, user_id, status, updated_at FROM course_task_store ORDER BY updated_at DESC LIMIT 10;"

# 查看定时任务明细（payload 里的 schedule 字段）
docker compose -p uh-test -f docker-compose.test.yml exec postgres \
  psql -U easylearning -d main_db -c \
  "SELECT task_id, status, payload->'schedule' FROM course_task_store WHERE status='scheduled';"
```

---

## 8. 故障排查

### 8.1 构建失败

**`npm ci` 报错 / 超时**
`.env` 里确认 `NPM_REGISTRY=https://registry.npmmirror.com`（国内服务器必需）。

**pip 安装超时**
`Dockerfile.server` 默认已用清华 PyPI 源。若仍超时，检查服务器出网。

**磁盘不足**
```bash
docker system df
docker system prune -a    # ⚠️ 会删除所有未使用镜像
```

### 8.2 `schema` 不是 `ok`

Postgres 只在**全新数据卷**上执行 `database/` 里的初始化脚本。若数据卷被复用或初始化中断，
可能缺 `users` 表或 `tenant_template` 库。

app 启动时会自动尝试修复（`DB_AUTO_BOOTSTRAP` 默认开启）。若仍失败，手动执行：

```bash
docker compose -p uh-test -f docker-compose.test.yml exec -T postgres \
  psql -U easylearning -d main_db < database/00-schema.sql

docker compose -p uh-test -f docker-compose.test.yml exec -T postgres \
  psql -U easylearning -d main_db < database/01-create_tenant.sql
```

最彻底的方式是清空数据卷重来（**会丢失所有数据**）：

```bash
docker compose -p uh-test -f docker-compose.test.yml down -v
docker compose -p uh-test -f docker-compose.test.yml up -d --build
```

### 8.3 注册返回 503 `TenantProvisioningError`

- 「数据库正忙」→ 有连接占着 `tenant_template`（比如正在跑备份），稍后重试
- 「没有建库权限（CREATEDB）」→ 用了外部 Postgres 且角色无建库权限；本栈自带的 Postgres 角色是超级用户，不会出现
- 「数据库暂时连不上」→ 检查 `uh-test-db` 容器状态

### 8.4 400 `InvalidHost`（提示 `Invalid host header`）

访问用的域名/IP 不在允许列表。把实际访问地址加进 `ALLOWED_HOSTS`：

```env
ALLOWED_HOSTS=uh.example.com,192.168.1.10
```

改完 `.env` 后必须**重建** app 容器（app 只在创建时读 `.env`）：

```bash
docker compose -p uh-test -f docker-compose.test.yml up -d --force-recreate app
```

> `ENV=production` 时 `ALLOWED_HOSTS=*` 会导致 app 拒绝启动。

### 8.5 前端页面打不开 / 白屏

```bash
# 确认 web 容器健康
docker compose -p uh-test -f docker-compose.test.yml ps web
# 绕过缓存访问
curl -H "Cache-Control: no-cache" http://127.0.0.1:8080/
# 看 web 日志
docker compose -p uh-test -f docker-compose.test.yml logs web
```

若前端构建产物有问题，重建 web：

```bash
docker compose -p uh-test -f docker-compose.test.yml up -d --build --no-deps web
```

### 8.6 定时任务不触发

排查顺序：

1. **app 是否单 worker**：多 worker 会导致调度分裂
   ```bash
   docker compose -p uh-test -f docker-compose.test.yml exec app ps aux | grep uvicorn
   # 必须看到 --workers 1
   ```
2. **调度循环是否存活**：日志中每 15 秒应有一次调度活动（无任务时较安静，可先建个 1 分钟后的任务观察）
3. **数据库里状态是否为 scheduled**：用 §7 的 SQL 查询
4. **`fire_at` 是否已解析**：`payload->'schedule'->>'fire_at'` 非空说明抖动已解析

### 8.7 端口冲突

```bash
# 查占用
ss -tlnp | grep -E ':(8000|8080)'
```

改 `.env` 里的 `APP_PORT` / `HTTP_PORT` 后重建：

```bash
docker compose -p uh-test -f docker-compose.test.yml up -d
```

### 8.8 查看容器为什么退出

```bash
docker compose -p uh-test -f docker-compose.test.yml logs --tail=200 app
docker inspect uh-test-app --format '{{.State.ExitCode}} {{.State.Error}}'
```

常见原因：`CREDENTIAL_ENCRYPTION_KEY` 为空或不是合法 Fernet 密钥（`ENV=production` 时拒绝启动）。

---

## 9. 红线与注意事项

1. **必须 `--workers 1`**
   定时调度循环、任务状态、签到会话都在单进程内存里。多 worker 会导致：
   - 调度循环在多进程里重复触发同一任务
   - 网页看到的任务状态与实际执行进程不一致
   `Dockerfile.server` 已硬编码 `--workers 1`，**不要修改**。

2. **`CREDENTIAL_ENCRYPTION_KEY` 必须备份**
   它加密定时任务落库的账号密码和题库 token。丢失后这些数据无法解密，
   所有定时任务恢复时会因「缺少凭据」而失败。

3. **`.env` 不要提交到 git**
   已在 `.gitignore` 中。同时不要提交 `frontend/dist/`、`node_modules/`、`.npm-cache/`。

4. **方案 A 不要求 `.git`，但要确认源码是本 fork**
   `deploy_server.sh` 的 `resolve_repo_root()` 只检查 4 个文件是否存在，`--build` 构建的是
   **当前目录**的源码，与是否有 `.git` 无关；zip 上传同样可用。
   真正要防的是**拿错源码**（clone 了上游）：用
   `grep -q dispatch_scheduled_tasks backend/app/services/course/chaoxing/learning_manager.py`
   确认含定时任务代码。

5. **`UPDATE_CHECK_ENABLED=false`（建议）**
   上游脚本生成的 `.env` 不含这一项，默认 `true`。应用会去查**上游**仓库的 releases，
   可能提示你升级到不含本 fork 改动的版本。建议显式关闭。

6. **`ENV=production` 的约束**
   - `CORS_ORIGINS` 不能含 `*`
   - `localhost` 以外的来源必须 `https://`
   - `ALLOWED_HOSTS` 不能为 `*`
   违反任一条，app 会拒绝启动。

7. **对外暴露前关闭调试端口**
   测试期开放的 `8000`（app 直连）和 `8080`（web）应仅限内网，
   公网访问统一走宝塔 nginx 的 443。

8. **合规提醒**
   本项目为学习通/智慧树自动化工具，定时 + 波动功能本身即是为降低平台风控的时间特征而设计。
   请自行承担使用风险，仅用于个人学习用途。

---

## 10. 部署检查清单

复制这份清单逐项打勾：

**两种方案都要先确认（最重要）**：

- [ ] 源码来自 **本 fork**（`git clone https://github.com/dabai214109/university-helper.git`，
      或用宝塔上传本 fork 的源码 zip —— **两种方式都可以**）
- [ ] **不是** clone 上游 `sweetcornna/university-helper`
- [ ] 已用 `grep -q dispatch_scheduled_tasks backend/app/services/course/chaoxing/learning_manager.py`
      确认含定时任务代码（这条比检查 `.git` 更可靠）

**选方案 A（官方脚本）时**：

- [ ] Docker + Compose v2 已安装
- [ ] 4 个必需文件齐全（`docker-compose.release.yml` + `database/` 三个）—— 脚本不会联网下载
- [ ] `bash scripts/deploy_server.sh --build --host <IP> --admin-email <邮箱> -y` 执行成功
- [ ] `.env` 里已追加 `UPDATE_CHECK_ENABLED=false`
- [ ] `curl http://127.0.0.1:8080/health` 返回 `"schema":"ok"`

**选方案 B（本文件 compose）时**：

- [ ] Docker + Compose v2 已安装
- [ ] `.env` 已从 `.env.test.example` 创建并填好
- [ ] `POSTGRES_PASSWORD` 已改为强密码
- [ ] `SECRET_KEY` 已生成（≥32 字符）
- [ ] `CREDENTIAL_ENCRYPTION_KEY` 已生成（Fernet 格式）
- [ ] `ADMIN_EMAILS` 已填注册用的邮箱
- [ ] `CORS_ORIGINS` 是 **JSON 数组**格式且与实际访问地址一致
- [ ] `docker compose -p uh-test -f docker-compose.test.yml up -d --build` 成功
- [ ] `curl http://127.0.0.1:8080/health` 返回 `"schema":"ok"`

**部署完成后都要确认**：

- [ ] 用域名/IP 访问时 `ALLOWED_HOSTS` 已填
- [ ] 防火墙 / 云安全组已放行端口
- [ ] 已注册账号且邮箱与 `ADMIN_EMAILS` 一致
- [ ] 导航栏能看到「管理面板」入口
- [ ] 定时任务创建后 15 秒内自动触发（§6.1）
- [ ] app 容器重启后定时任务仍为 `scheduled`（§6.3）
- [ ] `.env` 已备份到安全位置

---

## 附录 A：给宝塔 AI 的完整命令序列

### A.1 推荐：方案 A（官方脚本 `--build`，密钥自动生成）

```bash
# ---- 变量：请按实际情况替换 ----
SERVER_IP="1.2.3.4"          # 服务器公网 IP
ADMIN_EMAIL="you@example.com"
PROJECT_DIR="/www/wwwroot/university-helper"
FORK_URL="https://github.com/dabai214109/university-helper.git"

# ---- 1. 获取源码 ----
# 方式一：git clone（服务器能访问 GitHub 时）
# 方式二：让用户用宝塔文件管理器上传本 fork 的源码 zip 并解压到 $PROJECT_DIR
#         （git 长连接被阻断时用这个；脚本不要求 .git，方案 A 照样能用）
if [ -d "$PROJECT_DIR/.git" ]; then
  echo "✅ 源码已存在，拉取最新"
  git -C "$PROJECT_DIR" pull --ff-only || echo "⚠️ git pull 失败（网络？），继续用现有源码"
elif [ -f "$PROJECT_DIR/scripts/deploy_server.sh" ]; then
  echo "✅ 源码已存在（zip 上传，无 .git），跳过 git 操作"
else
  echo "克隆本 fork（不要 clone 上游 sweetcornna/university-helper）..."
  git clone "$FORK_URL" "$PROJECT_DIR" || {
    echo "❌ git clone 失败（国内长连接常被阻断）。"
    echo "   请让用户用宝塔文件管理器上传源码 zip 到 $PROJECT_DIR 后重跑。"
    exit 1
  }
fi
cd "$PROJECT_DIR" || exit 1

# ---- 2. 前置检查：确认是本 fork + 脚本不会联网下载 ----
grep -q "dispatch_scheduled_tasks" \
  backend/app/services/course/chaoxing/learning_manager.py \
  && echo "✅ 是本 fork（含定时任务代码）" \
  || { echo "❌ 不是本 fork！缺定时任务代码，请重新获取源码。"; exit 1; }

for f in docker-compose.release.yml database/00-schema.sql \
         database/02-bootstrap-tenant-template.sh database/templates/tenant_template.sql; do
  [ -f "$f" ] || { echo "❌ 缺少 $f —— 脚本会尝试联网下载，请检查源码完整性。"; exit 1; }
done
echo "✅ 源码完整（脚本不会联网下载，也不需要 .git）"

# ---- 3. 一键构建部署（自动生成 .env 密钥）----
bash scripts/deploy_server.sh --build --host "$SERVER_IP" \
  --admin-email "$ADMIN_EMAIL" -y

# ---- 4. 等待就绪 ----
for i in $(seq 1 60); do
  if curl -fsS http://127.0.0.1:8080/health 2>/dev/null | grep -q '"schema":"ok"'; then
    echo "✅ 部署成功"; break
  fi
  echo "等待中... ($i/60)"; sleep 5
done

# ---- 5. 验证 ----
DC="docker compose -p university-helper -f docker-compose.release.yml"
$DC ps
curl -fsS http://127.0.0.1:8080/health; echo ""
echo "--- 管理面板路由（401/403 = 正常挂载，404 = 代码没构建进去）---"
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/v1/admin/overview

echo ""
echo "访问地址：http://${SERVER_IP}:8080"
echo "管理员邮箱：${ADMIN_EMAIL}（用此邮箱注册）"
echo "⚠️ 请立即备份 ${PROJECT_DIR}/.env"
```

> 脚本默认把 `HTTP_BIND_HOST=0.0.0.0`（用 `--host` 时），即已对外监听；
> 记得在宝塔「安全」和云厂商安全组放行 `8080`。
>
> 脚本生成的 `.env` **不含** `UPDATE_CHECK_ENABLED`，默认 `true` —— 应用会去查
> **上游** `sweetcornna/university-helper` 的 releases，可能提示你升级到不含本 fork
> 改动的版本。要避免这个误导，在该 `.env` 末尾追加 `UPDATE_CHECK_ENABLED=false`
> 后重启 app。

### A.2 备选：方案 B（本文件的 compose，与生产隔离）

```bash
# ---- 变量：请按实际情况替换 ----
SERVER_IP="1.2.3.4"
ADMIN_EMAIL="you@example.com"
PROJECT_DIR="/www/wwwroot/university-helper"
FORK_URL="https://github.com/dabai214109/university-helper.git"

# ---- 1. 获取源码（git clone 或 zip 上传都可以）----
if [ -d "$PROJECT_DIR/.git" ]; then
  echo "✅ 源码已存在，拉取最新"
  git -C "$PROJECT_DIR" pull --ff-only || echo "⚠️ git pull 失败（网络？），继续用现有源码"
elif [ -f "$PROJECT_DIR/docker-compose.test.yml" ]; then
  echo "✅ 源码已存在（zip 上传，无 .git）"
else
  echo "克隆本 fork（不要 clone 上游 sweetcornna/university-helper）..."
  git clone "$FORK_URL" "$PROJECT_DIR" || {
    echo "❌ git clone 失败。请让用户上传源码 zip 到 $PROJECT_DIR 后重跑。"; exit 1; }
fi
cd "$PROJECT_DIR" || exit 1

# 确认是本 fork（含定时任务代码）
grep -q "dispatch_scheduled_tasks" \
  backend/app/services/course/chaoxing/learning_manager.py \
  && echo "✅ 是本 fork" || { echo "❌ 不是本 fork，请重新获取源码。"; exit 1; }

# ---- 2. 生成密钥 ----
JWT_SECRET=$(openssl rand -hex 32)
FERNET_KEY=$(docker run --rm python:3.11-slim sh -c \
  "pip install -q cryptography 2>/dev/null && python -c \"from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())\"")
DB_PASSWORD=$(openssl rand -base64 24 | tr -d '/+=' | head -c 32)

# ---- 3. 写 .env ----
cat > .env <<EOF
POSTGRES_PASSWORD=${DB_PASSWORD}
SECRET_KEY=${JWT_SECRET}
CREDENTIAL_ENCRYPTION_KEY=${FERNET_KEY}
ADMIN_EMAILS=${ADMIN_EMAIL}
CORS_ORIGINS=["http://${SERVER_IP}:8080"]
ALLOWED_HOSTS=${SERVER_IP}
APP_PORT=8000
HTTP_PORT=8080
HTTP_BIND_HOST=0.0.0.0
ENV=dev
UPDATE_CHECK_ENABLED=false
NPM_REGISTRY=https://registry.npmmirror.com
EOF

chmod 600 .env

# ---- 3. 构建并启动 ----
docker compose -p uh-test -f docker-compose.test.yml up -d --build

# ---- 4. 等待就绪（最多 5 分钟）----
for i in $(seq 1 60); do
  if curl -fsS http://127.0.0.1:8080/health 2>/dev/null | grep -q '"schema":"ok"'; then
    echo "✅ 部署成功"; break
  fi
  echo "等待中... ($i/60)"; sleep 5
done

# ---- 5. 最终验证 ----
echo "--- 容器状态 ---"
docker compose -p uh-test -f docker-compose.test.yml ps
echo "--- 健康检查 ---"
curl -fsS http://127.0.0.1:8080/health; echo ""
echo "--- 管理面板路由（401/403 = 正常挂载）---"
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/api/v1/admin/overview

echo ""
echo "访问地址：http://${SERVER_IP}:8080"
echo "管理员邮箱：${ADMIN_EMAIL}（用此邮箱注册）"
echo "⚠️ 请立即备份 ${PROJECT_DIR}/.env"
```

> 若第 4 步超时未就绪，执行
> `docker compose -p uh-test -f docker-compose.test.yml logs --tail=200 app` 查看原因，
> 常见问题是 `CREDENTIAL_ENCRYPTION_KEY` 生成失败或前端 npm 构建超时。
>
> 执行完请把「访问地址」「管理员邮箱」和健康检查结果报告给用户。

---

## 附录 B：切换到「宝塔 nginx 反代 + 域名 + HTTPS」

在附录 A 的基础上：

```bash
cd /www/wwwroot/university-helper

# 1. 改为仅回环绑定 + 生产校验
sed -i 's|^HTTP_BIND_HOST=.*|HTTP_BIND_HOST=127.0.0.1|' .env
sed -i 's|^ENV=.*|ENV=production|' .env
sed -i "s|^CORS_ORIGINS=.*|CORS_ORIGINS=[\"https://uh.example.com\"]|" .env
sed -i "s|^ALLOWED_HOSTS=.*|ALLOWED_HOSTS=uh.example.com|" .env

# 2. 重建 app 让新环境变量生效
docker compose -p uh-test -f docker-compose.test.yml up -d --force-recreate app

# 3. 然后在宝塔面板操作（无法用命令完成）：
#    - 网站 → 添加站点 uh.example.com（纯静态）
#    - 设置 → SSL → Let's Encrypt 申请证书 → 强制 HTTPS
#    - 设置 → 反向代理 → 目标 http://127.0.0.1:8080
#      并补上 WebSocket 与长超时配置（见 §4.3）

# 4. 关闭对外暴露的 8080
#    宝塔面板 → 安全 → 删除 8080 放行规则
#    云服务器控制台 → 安全组 → 删除 8080 入站规则

# 5. 验证
curl -fsS https://uh.example.com/health
```

---

## 附录 C：本 fork 相对上游的改动（便于核对部署是否成功）

| 层 | 文件 | 改动 |
|---|---|---|
| 后端 | `app/services/course/task_store.py` | 加密 `token`/`api_key` 及嵌套 `tiku_config` |
| 后端 | `.../chaoxing/learning_manager.py` | `scheduled` 状态、凭据/排期参数分离、`_public_view` 防泄露、`dispatch_scheduled_tasks`（15s 调度 + 抖动解析 + 到点自动暂停）、`reschedule_task` / `start_now_task` |
| 后端 | `app/main.py` | lifespan 注册 15s 调度循环 |
| 后端 | `app/api/v1/course.py` | `start_at`/`stop_at`/抖动字段与校验、`reschedule` 与 `start-now` 路由、状态响应带 `schedule` |
| 后端 | `app/api/v1/admin.py`（新增） | `/admin/overview`、`/admin/users`、`/admin/tasks`、`/admin/task/{id}/stop` |
| 前端 | `pages/Admin.jsx`（新增） | 管理面板页（KPI + 用户表 + 跨用户任务表） |
| 前端 | `chaoxing-fanya/components/ConfigSection.jsx` | 立即执行/定时启动切换、起止时间、抖动滑块 |
| 前端 | `chaoxing-fanya/components/TaskControlSection.jsx` | 定时徽标、改期、立即开始；定时态隐藏暂停 |
| 前端 | `App.jsx` / `AppLayout.jsx` | `/admin` 路由与导航入口（仅服务器模式） |

**验证部署成功的三个标志**：
1. `/health` 返回 `"schema":"ok"`
2. 导航栏有「管理面板」入口，`/admin` 能打开
3. 发起页有「立即执行 / 定时启动」选项，定时任务 15 秒内自动触发
